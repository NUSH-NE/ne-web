import { Card, CardActionArea } from '@material-ui/core';

export default function ArticleCard(p) {
    const { imgURL, title, summary, rating } = p;

    return <Card>
        <CardActionArea>

        </CardActionArea>
    </Card>
}